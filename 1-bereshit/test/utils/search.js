/**
 * search.js
 * קובץ זה מכיל את כל הפונקציות הקשורות לחיפוש בתוכן הפרקים
 *
 * הפונקציות הנדרשות להעתקה:
 * - searchChapters() - פונקציית החיפוש הראשית
 * - highlightSearchResults() - הדגשת תוצאות החיפוש
 * - debounce() - פונקציית עזר למניעת קריאות מיותרות
 */

import { chaptersData } from "../models/ChapterModel.js";
import { convertHebrewLetterToNumber } from "./converters.js";

// העתק לכאן את הפונקציות:
// - searchChapters()
// - highlightSearchResults()
// - debounce()

export { searchChapters, highlightSearchResults, debounce };
