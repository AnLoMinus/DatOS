/**
 * analytics.js
 * קובץ זה מכיל פונקציות לחישוב סטטיסטיקות ונתונים על הפרקים
 *
 * הפונקציות הנדרשות להעתקה:
 * - calculateStats() - חישוב סטטיסטיקות כלליות
 * - displayChapter() - הצגת מידע על פרק ספציפי
 * - calculateBookProgress() - חישוב התקדמות כתיבת הספר
 * - getDayNumber() - חישוב מספר היום לפי מספר פסוק
 */

import { bereshitChapters } from "../../data/chapters.jsrs.js";

// העתק לכאן את הפונקציות:
// - calculateStats()
// - displayChapter()
// - calculateBookProgress()
// - getDayNumber()

export { calculateStats, displayChapter, calculateBookProgress, getDayNumber };
